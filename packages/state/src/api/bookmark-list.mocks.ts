import { MOCK_BOOKMARKS } from './bookmark.mocks'
import type { ConversationBookmark } from './bookmark.types'

// Mock data for bookmark lists - references the same data as individual bookmarks
export const MOCK_BOOKMARKS_LIST: Record<string, ConversationBookmark> = MOCK_BOOKMARKS
